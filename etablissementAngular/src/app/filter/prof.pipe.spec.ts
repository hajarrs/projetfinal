/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ProfPipe } from './prof.pipe';

describe('Pipe: Profe', () => {
  it('create an instance', () => {
    let pipe = new ProfPipe();
    expect(pipe).toBeTruthy();
  });
});
