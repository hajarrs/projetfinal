/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SallePipe } from './salle.pipe';

describe('Pipe: Sallee', () => {
  it('create an instance', () => {
    let pipe = new SallePipe();
    expect(pipe).toBeTruthy();
  });
});
